# normai
# EC2 y RDS ya están funcionando juntas, en la base de datos de la RDS creo que ya están todas las tablas que teníamos hasta el momento (sin datos).
# La EC2 ya está conectada con el repositorio de Git y con Django instalado. Aunque aún no he conseguido ver la página como tal corriendo el servidor desde la EC2, creo que es un problema con la configuración de settings.py del projecto de Django (que está dentro de la carpeta /velzon) y los puertos de la EC2.
